	ЗАДАЧА
	Выяснить, является ли фигура кубом
Чтобы найти объем (в кубических сантиметрах) прямоугольного параллелепипеда, используйте формулу:
volume = Length * Width * Height
Но кто-то забыл использовать надлежащий учет, поэтому у нас есть только объем и длина одной стороны!
Вы должны выяснить, равны ли стороны прямоугольного параллелепипеда (= является кубом).
Возврат true, если кубоид может иметь равные стороны, возврат falseв противном случае.
Возврат false также для недопустимых чисел (например, объем или сторона меньше или равна 0).

Примечание: сторона будет целым числом

	РЕШЕНИЕ

def cube_checker(volume, side):
    return volume == side ** 3 and volume > 0 and side > 0


	ЗАДАЧА
	Удалить строковые пробелы

Напишите функцию, которая удаляет пробелы из строки, а затем возвращает результирующую строку.
Примеры:
Input -> Output
"8 j 8   mBliB8g  imjB8B8  jl  B" -> "8j8mBliB8gimjB8B8jlB"
"8 8 Bi fk8h B 8 BB8B B B  B888 c hl8 BhB fd" -> "88Bifk8hB8BB8BBBB888chl8BhBfd"
"8aaaaa dddd r     " -> "8aaaaaddddr"

	РЕШЕНИЕ
def no_space(x):
    return ''.join(x.split())

	ЗАДАЧА
	Чак Норрис VII - правда или ложь? (Начинающий)
Общеизвестно, что Чак Норрис получает все, чего хочет. В результате Чаку очень редко приходится использовать слово «ложь».
Менее известен тот факт, что если что-то правда, а Чак не хочет, чтобы это было правдой, Чак может напугать правду своими
массивными бицепсами, и она автоматически станет ложью.
Ваша задача - больше походить на Чака (ха! удачи!). Вы должны вернуть false, даже не используя слово false...
Иди, покажи правду, кто в доме хозяин!

	РЕШЕНИЕ
def if_chuck_says_so():
    return 0 != 0

	ЗАДАЧА
	Строка заканчивается?
Завершите решение так, чтобы оно возвращало true, если первый переданный аргумент (строка) заканчивается вторым аргументом (тоже строкой).
Примеры:
solution('abc', 'bc') # returns true
solution('abc', 'd') # returns false

	РЕШЕНИЕ
def solution(text, ending):
   return text[-len(ending)::] == ending





